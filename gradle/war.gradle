apply plugin: "war"

bootWar {
    mainClassName = "com.xcode.app.XcodeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
